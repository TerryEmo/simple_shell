#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

/**
 * _getline - Reads the entire line of intake from user
 * @lineptr: Pointer to the buffer
 * @stream: Size of the buffer
 * Return: Num of characters read, or -1 if an error occurred.
 */
ssize_t _getline(char **lineptr, FILE *stream)
{
	(void)lineptr;
	(void)stream;

	return (0);
}
